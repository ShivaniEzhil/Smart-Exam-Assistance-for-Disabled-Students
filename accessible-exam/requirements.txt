# Accessible Exam Tool — Python dependencies
# Install: pip install -r requirements.txt
# Run from: accessible-exam/ (see README.md)

# Web framework
flask>=2.0.0

# PDF question papers: extract text from uploaded PDFs
pypdf>=3.0

# Image question papers + OCR: extract text from images (PNG, JPG, etc.)
Pillow
pytesseract

# Sign language (Deaf mode): hand detection and gesture/word recognition
opencv-python-headless>=4.5.0,<4.6
numpy<2
protobuf<4
mediapipe

# -------------------------------------------------------
# Optional — only if you train sign-language models locally
# -------------------------------------------------------
# For letter model (train_alphabet_model.py) and word model (train_word_model.py):
scikit-learn>=1.0

# For CNN letter model (train_alphabet_cnn.py); skip if using action.pkl only:
# pip install tensorflow

# -------------------------------------------------------
# TTS & STT run in the browser (Web Speech API), not on the server.
# No pyttsx3, SpeechRecognition, or PyAudio needed for the app.
# -------------------------------------------------------
